spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
#    gateway:
#      discovery:
#         locator:
#           enabled: true
#      routes:
#      - id: AUTH-SERVICE
#        uri: lb://AUTH-SERVICE
#        predicates:
#        - Path=/AUTH-SERVICE/**
#        filters:
#        - Pach=/AUTH-SERVICE/**

