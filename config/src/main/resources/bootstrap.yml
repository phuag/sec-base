server:
  port: 8888
#  ssl:
#    key-store: classpath:httpskey/tomcat.keystore
#    key-store-password: tomcat

info:
  name: config
  version: 1.0-SNAPSHOT

spring:
  application:
    name: config
  boot:
    admin:
      client:
        url: http://localhost:8040
  cloud:
    config:
      server:
        native:
          search-locations:
           - file:///E:/study/workspace/wish-admin/config-repo
  profiles:
    active: native

eureka:
  client:
    service-url:
      defaultZoon: http://localhost:8761/

management:
  endpoints:
    web:
      exposure:
        include: "*"
    health:
      show-details: always